# GPXMapper

GPXMapper creates short videos from GPX tracks, with titles and captions, and reverse geolocation via Nominatim.

- Use the Reference section for API docs generated from the source code.
- See the CLI help: `gpxmapper --help`.

