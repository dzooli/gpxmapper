---
apply: always
---

# Redirect

Project instructions for automation are maintained in **`.cursor/rules/`** for **Cursor**:

- `.cursor/rules/gpxmapper-core.mdc` — always on
- `.cursor/rules/python-standards.mdc` — Python files
- `.cursor/rules/git-github-workflow.mdc` — git / GitHub

Update those files instead of this one to avoid drift.
